def findDuplicates(self, arr):
    # code here
    n = len(arr)
    ans = []
    for i in range(n):
        index = abs(arr[i])-1
        if(arr[index]<0):
            ans.append(index+1)
        arr[index]*=-1
    return ans
