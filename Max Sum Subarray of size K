class Solution:
    def maxSubarraySum(self, arr, k):
        # code here 
        m = 0
        n = len(arr)
        for i in range(k):
            m += arr[i]
        s = m
        for i in range(k,n):
            s=s-arr[i-k]+arr[i]
            m = max(m,s)
        return m
