class Solution:
    def findMoves(self, chairs, passengers):
        # code here
        chairs.sort()
        passengers.sort()
        n = len(chairs)
        result = 0
        for i in range(n):
            result+=abs(chairs[i]-passengers[i])
        return result
        
